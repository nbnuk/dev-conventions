# Active task (handoff pointer)

Use this file when **resuming multi-slice / TDD task work** in a new chat so the assistant opens the right handoff without guessing.

No active task.

**When to edit:** Point **Handoff** and **Task** at the `open/*.md` files you are actively working on. When nothing is in progress, remove the links or replace this section with a short note (e.g. “No active task”).

**When the whole task is finished:** Move the task (and optional handoff) under `done/`, update `completed.md`, then clear or update this pointer.
